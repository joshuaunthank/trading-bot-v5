services:  - type: web    name: trading-bot-v5    env: node    plan: free    buildCommand: npm install && npm run build    startCommand: npm run start    envVars:      - key: NODE_ENV        value: production      - key: BINANCE_API_KEY        sync: false      - key: BINANCE_API_SECRET        sync: false    healthCheckPath: /api/v1/strategies